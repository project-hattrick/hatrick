# React 19 peer ranges across the Solana wallet-adapter stack aren't all updated yet,
# so a strict install fails. Vercel (and `npm ci`) read this automatically — mirrors the
# `--legacy-peer-deps` the CI workflow passes explicitly.
legacy-peer-deps=true
