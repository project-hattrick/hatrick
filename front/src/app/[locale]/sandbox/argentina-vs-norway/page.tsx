import { ArgentinaVsNorwayArena } from '@/components/txline-arena/argentina-vs-norway-arena';

/** Argentina vs Norway 11v11 with all feel effects + the signature screen filter (no filter editor). */
export default function ArgentinaVsNorwayPage() {
  return <ArgentinaVsNorwayArena />;
}
