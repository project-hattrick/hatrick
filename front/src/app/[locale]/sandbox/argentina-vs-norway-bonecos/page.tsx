import { ArgentinaVsNorwayBonecoEditor } from '@/components/aligner/argentina-vs-norway-boneco-editor';

/** Live boneco editor (head/body geometry) over the Argentina vs Norway match — edits reflect on the players. */
export default function ArgentinaVsNorwayBonecosPage() {
  return <ArgentinaVsNorwayBonecoEditor />;
}
