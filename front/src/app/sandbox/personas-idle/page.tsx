import { IdleRegenPreview } from '@/components/aligner/idle-regen-preview';

export const metadata = {
  title: 'Regen Idle In-Game — Hat-trick',
};

/** Dev tool: the regen body-only idle pack composited with persona heads, in a real stage. */
export default function PersonasIdlePage() {
  return <IdleRegenPreview />;
}
