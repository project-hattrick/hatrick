/** Semantic color roles in the design system (mapped to tokens in tone.config). */
export enum Tone {
  Neutral = 'Neutral',
  Primary = 'Primary',
  Positive = 'Positive',
  Warning = 'Warning',
  Danger = 'Danger',
  Info = 'Info',
}
