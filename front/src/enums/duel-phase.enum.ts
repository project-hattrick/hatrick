/** Where the simulated duel match is on its 90' timeline (compressed into ~5 real minutes). */
export enum DuelPhase {
  FirstHalf = 'first_half',
  HalfTime = 'half_time',
  SecondHalf = 'second_half',
  FullTime = 'full_time',
}
