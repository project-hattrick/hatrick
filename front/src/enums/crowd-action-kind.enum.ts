/** Inline action a crowd message (HatBot) can carry — drives the CTA button. */
export enum CrowdActionKind {
  OpenBetSlip = 'open-bet-slip',
  OpenLogin = 'open-login',
  ShareMoment = 'share-moment',
}
