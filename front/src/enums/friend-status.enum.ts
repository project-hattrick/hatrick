/** Relationship between the signed-in user and another player, from self's POV. */
export enum FriendStatus {
  None = 'none',
  Outgoing = 'outgoing',
  Incoming = 'incoming',
  Friends = 'friends',
}
