import { CheckpointId, RuntimeKind, type CheckpointMeta } from '../types';

/** Checkpoint 4: "Cinema" — the Real Match GK runtime on a large, camera-roamed pitch. */
export const REAL_GK_V3_META: CheckpointMeta = {
  id: CheckpointId.RealGkV3,
  title: 'Checkpoint 4 — Cinema',
  subtitle: 'A vast pitch, tiny players & a dramatic dynamic-zoom camera',
  version: 'v3.0',
  createdAt: '2026-07-01',
  accent: 'realgk',
  runtime: RuntimeKind.RealGk,
};
