import { CheckpointId, RuntimeKind, type CheckpointMeta } from '../types';

/** Checkpoint 5: "Broadcast" — hero-sized actors, new anims, goal celebrations & TV slow-mo replays. */
export const REAL_GK_V4_META: CheckpointMeta = {
  id: CheckpointId.RealGkV4,
  title: 'Checkpoint 5 — Broadcast',
  subtitle: 'Goal celebrations, TV wipes & slow-motion replays focused on the ball',
  version: 'v4.0',
  createdAt: '2026-07-01',
  accent: 'realgk',
  runtime: RuntimeKind.RealGk,
};
